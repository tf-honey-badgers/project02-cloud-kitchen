<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Config 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.badgers.mapper.MenuMapper">
	
	<update id="updateMenuInfo">
		update menu set photo=#{mPhoto}, basic_price=#{mBasicPrice}, name=#{mName} where idx= #{mCode}
	</update>
	
	<update id="updateMenuOptInfo" >
		update menu_option set name=#{moName}, add_price=#{moAddPrice} where idx= #{moCode}
	</update>
	
	
	
</mapper>